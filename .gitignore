# Secrets
.env
.spotify-cache
# spotipy's default cache path — falls back to this if `cache_path` isn't
# passed explicitly. Holds an access token when running under Client
# Credentials, so keep it out of the repo.
.cache

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
uv.lock

# Editors / OS
.vscode/
.idea/
.DS_Store

# Logs
*.log
